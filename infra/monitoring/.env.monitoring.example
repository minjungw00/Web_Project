###############################################
# Monitoring Layer env (example)
# Copy to infra/monitoring/.env.monitoring.dev for dev.
# Copy to $SERVER_ROOT/monitoring/.env.monitoring.prod on the server.
# Use __REPLACE_ME__ placeholders for secrets when sharing.
###############################################

# --- Grafana ------------------------------------------------
GRAFANA_ADMIN_USER=admin
GRAFANA_ADMIN_PASSWORD=__REPLACE_ME__
GRAFANA_ROOT_URL=https://monitoring.example.com/monitoring/grafana/

# --- Alertmanager integrations ------------------------------
# Leave Slack variables empty if the integration is not used.
ALERTMANAGER_SLACK_WEBHOOK_URL=
ALERTMANAGER_SLACK_CHANNEL=#web-project-alerts
ALERTMANAGER_EMAIL_TO=ops@example.com
ALERTMANAGER_SMTP_HOST=smtp.example.com:587
ALERTMANAGER_SMTP_FROM=monitor@web-project.dev
ALERTMANAGER_SMTP_USERNAME=monitor
ALERTMANAGER_SMTP_PASSWORD=__REPLACE_ME__

# --- Exporters ----------------------------------------------
# Store mysqld-exporter credentials in mysqld-exporter/.my.cnf.* (gitignored).
# Use MYSQL_EXPORTER_ADDRESS to point to the MySQL host and port.
MYSQL_EXPORTER_ADDRESS=mysql:3306
NGINX_EXPORTER_SCRAPE_URI=http://nginx/nginx_status

# --- Data volumes -------------------------------------------
PROMETHEUS_DATA_MOUNT=prometheus-data
ALERTMANAGER_DATA_MOUNT=alertmanager-data
GRAFANA_DATA_MOUNT=grafana-data
LOKI_DATA_MOUNT=loki-data
PROMTAIL_POSITIONS_MOUNT=promtail-positions