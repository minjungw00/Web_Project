apiVersion: 1

providers:
  - name: web-project
    orgId: 1
    folder: Web Project
    folderUid: web-project
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    options:
      path: /etc/grafana/provisioning/dashboards
