###############################################
# Infrastructure Layer env (example)
# Copy to infra/infrastructure/.env.infrastructure.dev for dev.
# Copy to $SERVER_ROOT/infrastructure/.env.infrastructure.prod on the server.
# Use __REPLACE_ME__ placeholders for secrets when sharing.
###############################################

# --- MySQL image settings ----------------------------------
MYSQL_IMAGE=mysql
DB_TAG=8.4
MYSQL_PORT=3306

# --- MySQL credentials (override on the server) ------------
MYSQL_ROOT_PASSWORD=__REPLACE_ME__
MYSQL_DATABASE=appdb
MYSQL_USER=app
MYSQL_PASSWORD=__REPLACE_ME__

# --- Volume mounts -----------------------------------------
# Use host bind mounts in production to persist data outside containers.
MYSQL_DATA_MOUNT=mysql-data
MYSQL_INIT_MOUNT=./mysql/init
