###############################################
# Infrastructure Layer env (example)
# - Copy to infra/infrastructure/.env.infrastructure.dev for dev
# - Copy to server as $SERVER_ROOT/infrastructure/.env.infrastructure.prod for prod
# - Secrets policy: use __REPLACE_ME__ for secrets; do not commit real values
###############################################

## MySQL settings
MYSQL_IMAGE=mysql
DB_TAG=8.4
MYSQL_PORT=3306

## MySQL credentials (set on server for prod)
MYSQL_ROOT_PASSWORD=__REPLACE_ME__
MYSQL_DATABASE=appdb
MYSQL_USER=app
MYSQL_PASSWORD=__REPLACE_ME__

## Volumes (use host bind mounts in production)
MYSQL_DATA_MOUNT=mysql-data
MYSQL_INIT_MOUNT=./mysql/init
