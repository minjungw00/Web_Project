# Sample htpasswd entries for /monitoring Basic Auth
# Copy to monitoring.htpasswd and replace the hash with the value generated by
# `openssl passwd -apr1 '<password>'`
# Format: <username>:<apr1-hash>
# Example placeholder (do not use in production):
# monitor:$apr1$placeholder$zzzzzzzzzzzzzzzzzzzzZ1
