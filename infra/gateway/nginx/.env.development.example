###############################################
# Nginx runtime env (example, development)
# - Used by docker-compose.gateway.dev.yml
###############################################

NGINX_STATIC_ROOT=/usr/share/nginx/html
NGINX_BACKEND_HOST=backend-blue
NGINX_BACKEND_PORT=8080
# Optional monitoring upstreams
GRAFANA_HOST=grafana
PROMETHEUS_HOST=prometheus
ALERTMANAGER_HOST=alertmanager
LOKI_HOST=loki

# Basic Auth (ensure monitoring stack alerts using internal network bypass)
NGINX_MONITORING_AUTH_REALM="Monitoring Portal"
NGINX_MONITORING_AUTH_FILE=/etc/nginx/secrets/monitoring.htpasswd
# Set to your dev Docker network CIDR (common default is 172.18.0.0/16)
NGINX_MONITORING_INTERNAL_CIDR=172.18.0.0/16