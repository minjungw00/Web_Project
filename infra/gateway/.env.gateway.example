###############################################
# Gateway Layer env (example)
# Copy to infra/gateway/.env.gateway.dev for dev.
# Copy to $SERVER_ROOT/gateway/.env.gateway.prod on the server.
# Values are mostly paths and image tags, so secrets are uncommon here.
###############################################

# --- Volumes ------------------------------------------------
CERTBOT_MOUNT=${HOME}/srv/web_project/certbot
LETSENCRYPT_MOUNT=${HOME}/srv/web_project/letsencrypt/etc
LETSENCRYPT_LOG_MOUNT=${HOME}/srv/web_project/letsencrypt/log

# --- Nginx image -------------------------------------------
NGINX_IMAGE=ghcr.io/minjungw00/web-project-nginx
NGINX_TAG=latest

# --- Basic Auth secret -------------------------------------
NGINX_MONITORING_HTPASSWD_FILE=${HOME}/srv/web_project/nginx/monitoring.htpasswd
