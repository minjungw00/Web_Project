###############################################
# Server Root env (example)
# - Copy to $HOME/srv/web_project/.env on the server
# - Contains global defaults used by all stacks
# - Layer-specific settings should live in each stack .env file:
# - infra/infrastructure/.env.infrastructure.{dev|prod}
# - infra/application/.env.application.{dev|prod}
# - infra/monitoring/.env.monitoring.{dev|prod}
# - infra/gateway/.env.gateway.{dev|prod}

## Note: Prefer defining variables in stack .env files; this file provides global defaults.

## Server root path (for deployment scripts)
SERVER_ROOT=${HOME}/srv/web_project

## Default image registry (if not specified in layer files)
IMAGE_REGISTRY=ghcr.io/minjungw00

## Global DNS settings (can be overridden in layer files)
COMPOSE_DNS1=1.1.1.1
COMPOSE_DNS2=8.8.8.8

## Docker Network Name
APP_NETWORK_NAME=web_project_webnet

# Shared volume names (override when running docker compose via --env-file)
# These names are used across multiple compose stacks to share data.

# Frontend dist (shared between application and gateway)
# - Leave FE_DIST_MOUNT empty to use the shared named volume below
# - Set FE_DIST_MOUNT to an absolute path to use a bind mount instead
FE_DIST_MOUNT=frontend-dist
FE_DIST_PATH=/opt/dist

# Optional: database data (not cross-compose, but configurable)
# MYSQL_DATA_MOUNT=
# MYSQL_DATA_VOLUME_NAME=web_project_mysql-data
