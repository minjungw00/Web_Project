###############################################
# Application Layer env (example)
# Copy to infra/application/.env.application.dev for local dev.
# Copy to $SERVER_ROOT/application/.env.application.prod on the server.
# Do not commit real secrets; use __REPLACE_ME__ placeholders when sharing.
###############################################

# --- Image settings (CI may override tags) -----------------
FRONTEND_IMAGE=ghcr.io/minjungw00/web-project-frontend
BACKEND_IMAGE=ghcr.io/minjungw00/web-project-backend
FE_TAG=latest
BE_TAG=latest
