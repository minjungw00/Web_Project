import '@/shared/styles/page.css';

const CSDocsPage = (): React.ReactElement => (
  <div className="page-stack">
    <section className="page-hero">
      <h1>CS Docs</h1>
      <p>Knowledge graph based on neuron-synapse connections.</p>
    </section>
  </div>
);

export default CSDocsPage;
