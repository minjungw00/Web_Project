export interface HomeSection {
  to: string;
  title: string;
  description: string;
  iconName: 'portfolio' | 'blog' | 'docs' | 'miniGames';
}

export interface HomeSectionDto {
  to: string;
  title: string;
  description: string;
  iconName: 'portfolio' | 'blog' | 'docs' | 'miniGames';
}
